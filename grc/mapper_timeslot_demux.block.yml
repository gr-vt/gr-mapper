# auto-generated by grc.converter

id: mapper_timeslot_demux
label: Timeslot Demux
category: '[mapper]'

parameters:
-   id: nusers
    label: Number of Timeslots
    dtype: int
    default: '2'
-   id: syncd
    label: Already synchronized
    dtype: enum
    default: 'True'
    options: ['True', 'False']
-   id: ts_id
    label: Timeslot Identifier
    dtype: int_vector
    default: '[0,0,0,0]'
-   id: ts_len
    label: Timeslot Length
    dtype: int

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import mapper
    make: mapper.timeslot_demux(${nusers}, ${syncd}, ${ts_id}, ${ts_len})

file_format: 1
