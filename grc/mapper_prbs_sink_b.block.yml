# auto-generated by grc.converter

id: mapper_prbs_sink_b
label: PRBS Sink B
category: '[mapper]'

parameters:
-   id: which_mode
    label: PRBS Sequence
    dtype: enum
    default: '"PRBS31"'
    options: ['"PRBS7"', '"PRBS15"', '"PRBS23"', '"PRBS31"']
    option_labels: [PRBS7, PRBS15, PRBS23, PRBS31]
-   id: reset_len
    label: reset_len
    dtype: int
    default: '100000'

inputs:
-   domain: stream
    dtype: byte

templates:
    imports: import mapper
    make: mapper.prbs_sink_b(${which_mode}, ${reset_len})

file_format: 1
