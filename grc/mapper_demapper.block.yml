# auto-generated by grc.converter

id: mapper_demapper
label: Symbol to Bit Hard Demapper
category: '[mapper]'

parameters:
-   id: modtype
    label: Modulaton
    dtype: enum
    options: [mapper.BPSK, mapper.P2BPSK, mapper.QPSK, mapper.PSK8]
    option_labels: [BPSK, Pi/2BPSK, QPSK, 8PSK]
-   id: symbol_values
    label: Symbol Values
    dtype: int_vector
    default: '[0,1]'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import mapper
    make: mapper.demapper(${modtype}, ${symbol_values})

file_format: 1
