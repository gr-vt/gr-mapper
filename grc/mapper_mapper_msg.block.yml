# auto-generated by grc.converter

id: mapper_mapper_msg
label: Bit to Symbol Mapper (Async Msg)
category: '[mapper]'

parameters:
-   id: modtype
    label: Modulaton
    dtype: enum
    options: [mapper.BPSK, mapper.P2BPSK, mapper.QPSK, mapper.PSK8, mapper.QAM16,
        mapper.QAM64, mapper.PAM4]
    option_labels: [BPSK, Pi/2BPSK, QPSK, 8PSK, QAM16, QAM64, PAM4]
-   id: symbol_values
    label: Symbol Values
    dtype: int_vector
    default: '[0,1]'

inputs:
-   domain: message
    id: pdus
    optional: true

outputs:
-   domain: message
    id: cpdus
    optional: true

templates:
    imports: import mapper
    make: mapper.mapper_msg(${modtype}, ${symbol_values})

file_format: 1
