# auto-generated by grc.converter

id: mapper_preamble_strip
label: Preamble Strip
category: '[mapper]'

parameters:
-   id: user_len
    label: Preable Length
    dtype: int
    default: '10'
-   id: synch
    label: Preamble
    dtype: int_vector
    default: '[0,0,0,0]'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import mapper
    make: mapper.preamble_strip(${user_len}, ${synch})

file_format: 1
