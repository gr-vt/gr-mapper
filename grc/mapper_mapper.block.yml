# auto-generated by grc.converter

id: mapper_mapper
label: Bit to Symbol Mapper
category: '[mapper]'

parameters:
-   id: modtype
    label: Modulaton
    dtype: enum
    options: [mapper.BPSK, mapper.P2BPSK, mapper.QPSK, mapper.PSK8]
    option_labels: [BPSK, Pi/2BPSK, QPSK, 8PSK]
-   id: symbol_values
    label: Symbol Values
    dtype: int_vector
    default: '[0,1]'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import mapper
    make: mapper.mapper(${modtype}, ${symbol_values})

file_format: 1
